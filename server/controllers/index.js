const doctors = require('./doctors');
const services = require('./services');
const appointments = require('./appointments');
const bookings = require('./bookings');

module.exports = {
    doctors, services, appointments, bookings
};